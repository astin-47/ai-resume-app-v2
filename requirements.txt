# =============================================================
# requirements.txt  —  Python packages for AI Resume Builder
# =============================================================
#
# Install locally with:  pip install -r requirements.txt
# On Render: installed automatically during deployment
# =============================================================

# ── Web framework ─────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0     # ASGI server that runs FastAPI
python-multipart==0.0.9       # required for file uploads (Form + File)

# ── File parsing ───────────────────────────────────────────
PyPDF2==3.0.1                 # extract text from PDF uploads
python-docx==1.1.2            # read DOCX uploads + generate styled DOCX output
lxml>=4.9.0                   # required by python-docx for XML operations

# ── PDF export ─────────────────────────────────────────────
# PDF is generated by LibreOffice on Render (Linux server).
# docx2pdf is kept as a LOCAL fallback for Windows/Mac development.
# If neither is available, the app returns DOCX instead of PDF.
docx2pdf>=0.1.8

# ── AI ─────────────────────────────────────────────────────
openai>=1.52.0                # OpenAI SDK — 1.52+ fixes the 'proxies' TypeError

# ── Environment variables ──────────────────────────────────
python-dotenv==1.0.1          # loads .env file into os.environ

# ── Async helpers ──────────────────────────────────────────
aiofiles==23.2.1              # async file I/O used internally by FastAPI
